name: '🧐 Gemini Pull Request Review'

on:
  pull_request:
    types:
      - 'opened'
      - 'reopened'

jobs:
  test-workflow:
    runs-on: ubuntu-latest
    steps:
      - name: 'Say Hello'
        run: echo "Hello World"
